# Bastet Agent Sync — 需求與接續設計

狀態：已授權開始開發；同步與跨電腦恢復尚待實作驗證。實作路線見 [Master plan](docs/MASTER_PLAN.md)。

GitHub 儲存庫：https://github.com/yamantaka520/Bastet-Agent-Sync

## 已確認的使用者需求

- 透過 Google Drive 共用資料夾，將本機 agent 資料同步至其他電腦上的相同 agent。
- 支援 Claude、Claude Code、Codex、Pi Agent、Agy CLI、Grok Build CLI。
- 第一版必須包含換電腦後接續原本對話，不能只同步設定、技能與記憶。
- 各 agent 資料隔離，跨 agent 格式轉換不屬於已確認需求。
- 提供桌面 GUI，支援 macOS、Windows、Linux。
- 支援繁體中文、簡體中文、英文、韓文、日文，可手動切換語系。
- 可常駐系統匣／選單列，關閉主視窗後可繼續同步。
- 自動偵測 Claude、Claude Code、Codex、Google Agy CLI、Grok Build CLI、Pi Agent；可逐一勾選或全選。
- 提供 Google Drive 同步目錄設定引導。
- 同步頻率可自訂，另提供自動接近即時模式。
- 設定完成後，使用者按「啟動同步」才開始同步；目標為雙向同步。

## GUI 與首次設定流程

1. 語言與裝置：初始跟隨系統語系，無對應時使用英文；可切換 `zh-TW`、`zh-CN`、`en`、`ko`、`ja`，設定裝置顯示名稱。
2. 偵測來源：搜尋已知安裝位置、使用者設定目錄、PATH 與適用的環境變數。顯示 agent 名稱、版本、資料路徑、對話數及適配器支援程度。CLI 存在不等於對話可搬移；僅有歷史資料的 agent 也要可被發現。提供重新掃描、手動指定路徑、逐一勾選、全選已偵測項目。同款 agent 的多個 profile 分開列出，未選取者不匯出也不匯入。
3. 設定目標：引導登入 Google、選擇或建立同步資料夾；加入其他電腦時選擇同一個同步空間。驗證可用權限與空間身分，顯示帳號、資料夾、既有裝置及資料概況。設定頁不自動上傳 agent 資料。
4. 同步選項：選擇雙向、僅上傳或僅下載；預設建議雙向。選擇接近即時、自訂間隔或手動模式。設定同步資料類別、專案路徑對應與衝突處理。
5. 檢查並啟動：顯示來源、目標、方向、頻率及對話恢復限制；至少選定一個可用來源且目標驗證成功後，啟用「啟動同步」。首次雙向同步以合併盤點為起點，不能將某台電腦缺少資料解讀成刪除。

主畫面包含同步總覽、Agent 清單、對話與接續、裝置與路徑、衝突與還原、設定。總覽提供「啟動／暫停同步」「立即同步」「開啟同步資料夾」，顯示每個 agent 的進度、待傳輸項目、最後成功時間與錯誤原因。

同步狀態區分未設定、待啟動、同步中、已同步、已暫停、離線等待、需重新登入、等待 agent 停止寫入、發現衝突及失敗。離線時保留佇列，恢復連線後自動重試；暫停或退出後不可重新排程傳輸。暫停需在安全邊界結束正在進行的套用。

## 常駐與多語系

- macOS 使用選單列圖示，Windows 使用通知區系統匣，Linux 使用桌面環境支援的 tray／StatusNotifier。Linux 缺少系統匣功能時保留可操作視窗，不能關閉後讓使用者找不到程式。
- 系統匣提供開啟視窗、同步狀態、立即同步、暫停／恢復與結束程式。
- 關閉視窗與結束程式的行為分開；提供「登入後啟動」選項，初始不自動啟用。
- 視窗、系統匣、設定引導、通知、錯誤與復原操作均涵蓋五種語系。保留 agent 原始對話與檔名，不翻譯使用者資料。
- 自動同步只在重大失敗、登入失效或需處理衝突時通知，例行成功以介面狀態呈現。

## Google Drive 連接方式（設計方案）

為滿足三平台需求，規劃直接使用 Google Drive API 為主要傳輸方式；既有 Drive 本機同步資料夾為可選方式。Google 官方 Drive 桌面版不提供 Linux 版本，因此不能將官方桌面同步程式作為必要依賴。

- 直接連接：透過系統瀏覽器進行桌面 OAuth 授權，使用 PKCE 與 state 驗證，token 儲存在本機憑證儲存區。引導選擇／建立雲端資料夾，以 folder ID 識別，避免同名目錄混淆。發布前需配置產品 OAuth client，驗證既有共用資料夾的權限與 API scope；一般使用者流程不要求自行開發 Google Cloud 專案。
- 本機資料夾：使用檔案選擇器選取已同步的 Drive 目錄，說明如何在另一台電腦選擇同一個雲端目錄的本機位置。只知道封包已寫入本機時，狀態顯示「已交給 Drive」，不可誤報另一台電腦已收到。Linux 僅在使用者已有同步／掛載工具時提供此模式。
- 同一裝置對同一同步空間只使用一個傳輸方式。切換方式要重新核對同步空間 ID 與快照紀錄，避免循環上傳。
- 使用者按啟動後才執行首次資料同步；OAuth 授權與目錄驗證屬設定流程。單向模式可用於備份或唯讀接收，雙向需要目標讀寫權限。

官方依據：

- [Google Drive 系統需求](https://support.google.com/drive/answer/2375082?hl=en)
- [Google 桌面 OAuth 授權流程](https://developers.google.com/identity/protocols/oauth2/native-app)

## 同步排程與雙向規則（設計方案）

- 接近即時：本機檔案監看加上事件合併與穩定性檢查；初始建議 debounce 3 秒，雲端變更檢查間隔 15 秒並依配額、錯誤與閒置退避。這是預設方案，實際延遲受網路、Drive 傳輸與 session 快照條件影響，不承諾固定秒數到達。
- 自訂間隔：提供秒／分鐘輸入及常用選項，限制合法範圍；排程不得重疊。另有「立即同步」供手動觸發。
- 雙向以每台裝置最後成功套用的共同版本為基準，追蹤來源與快照 ancestry。遠端匯入造成的本機變更不能無限重複上傳。
- 無衝突且適配器允許的變更自動套用；設定同欄位衝突需保留兩版供選擇，對話分岔則保留各分支。
- 收到遠端快照時，正在使用的本機 session 不被熱覆寫，顯示待套用原因。
- 取消選取 agent 只停止後續同步，不刪除其本機或雲端資料。來源暫時不存在、Drive 未掛載、權限錯誤不能當作刪除。
- 程式可即時同步已完成的對話快照；執行中 agent 的工作仍在原電腦，不能由檔案同步遷移其程序。

## 接續體驗

使用者在電腦 B 選擇 agent 和從電腦 A 同步來的對話，指定本機專案路徑後接續。顯示來源電腦、快照時間、完整性、agent 版本、專案狀態，以及接續模式。

1. 原生恢復：匯入可驗證的原生資料與必要索引，以原 agent 的 resume 機制恢復。必須以實際跨機測試通過後才標示支援。
2. 上下文接續：當原生格式不支援搬移時，保留可匯出的原始紀錄，另外建立新對話，帶入任務目標、決策、進度、相關檔案和未完成事項。明確標示為新對話，不得冒充原 session 恢復；這是降級方式，不能當作原生恢復驗收通過。
3. 不支援：無法取得足夠上下文或匯入介面時，顯示具體缺少的能力，不提供無效的接續按鈕。

完整歷史的保存與模型實際載入的上下文是不同概念。模型仍受上下文長度限制；不可承諾每次接續會將全部紀錄塞入模型。

## 同步資料

- 可攜設定、指令、skills、agent 定義、記憶和 MCP 定義；機器專屬欄位留在本機。
- 原生對話紀錄、分支關係、工具呼叫及結果、壓縮摘要，以及恢復所需的非敏感中繼資料。
- 被對話引用的附件與檔案依賴：可攜本機檔案依選取範圍封裝，無法取得的雲端附件明確列出。
- 專案身分、Git commit、分支、工作目錄對應；未提交變更提供選擇性工作快照。接收端先比較，禁止直接覆蓋既有工作。
- 不搬移執行中的程序、PTY、socket、鎖定檔或整個執行環境。外部 MCP 服務與本機依賴需在接收端可用。
- 登入憑證與 API key 預設不匯出，設定需逐欄處理。對話和附件也可能含秘密；憑證檔排除不等同完整內容去敏。正式自動同步前需定義加密與金鑰管理。
- AgentMemoryOS 之類外部記憶服務使用其匯出／匯入能力或既有共享服務，不直接同步執行中的資料庫。

## 傳輸與一致性設計

每台電腦的同步程式先產生不可變快照，再放入 Drive 資料夾。快照含 manifest、來源裝置 ID、agent 與格式版本、對話 ID、父快照 ID、檔案大小及雜湊。接收端等待所有資料到齊且通過校驗，先暫存、備份，再套用。

- Drive 檔案到達順序不可靠；即使看到完成標記，也必須驗證全部內容。
- 不將正在使用的 agent 資料目錄直接改成 Drive 目錄或 symlink。
- 不以 Drive 上的鎖定檔作為可靠的跨機互斥鎖。
- 同一對話在兩台電腦繼續時，建立兩個分支；保留各自的工具呼叫順序，不自動串接或以最後寫入覆蓋。
- 自動匯出需取得一致性快照；格式無法安全熱備份時，等待該 session 停止寫入。匯入也需避開本機正在使用的 session。
- 修改、刪除、重試都需可追蹤且具冪等性。刪除使用 tombstone 與保留期，避免離線設備讓刪除資料重新出現。
- 接收端將同步封包視為不可信輸入：檢查路徑越界、symlink、大小限制及格式版本。技能、hook 或 MCP 設定的套用須遵守接收端信任與權限設定。

## 本機能力探查（2026-09-05）

以下僅由本機 CLI help 確認接續入口，尚未證明資料格式或跨機移植相容性。

| Agent | 已觀察的入口 | 待驗證 |
| --- | --- | --- |
| Claude | 尚未確認，暫以 Claude Desktop 理解 | 產品身分、本機／雲端紀錄、可用匯入介面 |
| Claude Code | `claude --resume <session-id>` | 原生 transcript、專案路徑編碼、索引、附件、跨版本 |
| Codex | `codex resume <session-id>`，`-C` 指定工作目錄 | CLI／桌面版範圍、rollout 與索引、附件、跨版本 |
| Pi Agent | `pi --session <path\|id>`，`--session-dir` | session tree、cwd、擴充套件相依與分支 |
| Agy CLI | `agy --conversation <id>` | 本機儲存或後端依賴、匯出／匯入、專案身分 |
| Grok Build CLI | `grok --resume <id>`；`grok export` | 原生儲存與匯入、程式碼快照、專案路徑 |

Grok help 指出對話恢復與程式碼快照恢復為不同操作。所有適配器均須明確區分兩者。

## 驗收要求

1. 在 A 建立對話、留下可驗證的決策與一個工具結果；同步到不同使用者路徑的 B 後，能找到並接續。
2. 原生恢復須驗證對話身分、順序、分支與工具結果；上下文接續須明確顯示降級，保留歷史供檢索。
3. 測試 Drive 延遲、檔案部分到達、離線修改、重複傳輸、毀損封包與中斷恢復。
4. 兩台同時接續產生可辨識分支，無對話或本機工作遺失。
5. 匯入前發現專案版本不符、缺少附件或依賴時，顯示具體差異。
6. 不覆寫有效的本機登入資料；匯出測試確認已知憑證欄位排除。
7. 各 agent 的支援狀態依跨機測試結果呈現，不以 CLI 存在 resume 旗標代替驗收。
8. 三平台各自完成安裝、啟動、設定引導、常駐、退出與重新啟動測試；Linux 檢查無系統匣的降級操作。
9. 五語系涵蓋視窗、系統匣、通知和錯誤；語系切換保留同步設定，長文字不截斷主要操作。
10. 偵測來源支援有／無 CLI、多 profile、非預設路徑；未選取的 agent 不傳輸資料。
11. 設定完成但尚未按啟動時不傳輸 agent 資料；暫停、自訂間隔、立即同步及離線重試符合介面狀態。
12. 兩台以同一個 Drive 空間雙向同步，驗證無迴圈上傳、無靜默覆寫，以及來源消失不誤刪。

## 尚待決定

- 三個作業系統的最低版本、CPU 架構與 Linux 發行版驗證範圍。
- Claude 是否指 Claude Desktop；Codex 是否同時涵蓋桌面版與 CLI。
- GUI 技術框架、安裝包簽章與更新方式；Drive 共享成員與加密金鑰分發。
- 工作檔案和附件同步範圍、大小與保存期限。

可先獨立實作同步核心與模擬資料測試；實際啟用雲端資料傳輸需要已設定的目的資料夾。

## Google Drive 引導補充（2026-09-05）

使用者要求逐步設定精靈；中途中斷自動記錄已完成步驟，重新開啟可接續、重新開始，或改為完全手動設定。引導與手動模式共享已驗證的進度；重新開始保留舊設定紀錄，不刪除 Drive 資料或金鑰。設定完成與啟動 Agent 同步必須分開。實作契約見 [Setup wizard](docs/SETUP_WIZARD.md)。

## ChatGPT／Codex 整合版範圍（2026-09-05）

使用者詢問新版 ChatGPT 與 Codex 整合後的支援。適配器範圍納入整合版桌面程式中的本機 Codex／Worktree；ChatGPT Work 本機任務列入格式與接續能力驗證。依資料來源與執行環境辨識，不以應用程式名稱或同一側欄推定儲存格式相同。一般 ChatGPT 聊天、ChatGPT Work 雲端與 Codex Cloud 另行研究正式匯出／匯入或連結接續能力，尚不宣稱可透過本機目錄同步原生恢復。

官方依據：[Codex environments](https://learn.chatgpt.com/docs/environments/modes)、[Use ChatGPT](https://learn.chatgpt.com/docs/use-chatgpt)、[Projects and chats](https://learn.chatgpt.com/docs/projects)。截至本次記錄，只有來源路徑偵測與設計範圍，原生對話跨機恢復仍未驗收。

## Agent Memory OS 同步來源（2026-09-05）

使用者明確要求加入自有 Agent Memory OS。將其列為獨立的第七個同步來源，支援預設／環境變數／自訂目錄，使用正式 JSONL 同步封包，保留記憶、關聯、profile 與權限結構。禁止直接搬移執行中的資料庫。受信任匯入、刪除／ACL 預覽與備份須另行驗收；目前實作範圍見 [Agent Memory OS adapter](docs/AGENT_MEMORY_OS.md)。
